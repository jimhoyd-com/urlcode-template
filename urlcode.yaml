# yaml-language-server: $schema=https://raw.githubusercontent.com/jimhoyd-com/urlcode/v0.4.1/schemas/urlcode.schema.json
# Organize files however you like; list their paths from this project root.
version: "1"
includes:
  - routes/functions.yaml
  - routes/marketing/links.yaml
routes: {}
